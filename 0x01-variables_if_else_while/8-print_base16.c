#include <stdio.h>
/**
 * main - is entry of program
 *
 * Return: 0, if succesful
 */
int main(void)
{
int num;
char al;

for (num = 0 ; num < 10 ; num++)
putchar((x % 10) + '0');

for (al = 'a' ; al <= 'f' ; al++)
putchar(al);
putchar('\n');
return (0);
}
