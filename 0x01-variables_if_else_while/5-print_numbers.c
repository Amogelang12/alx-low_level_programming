#include <stdio.h>
#include <ctype.h>
/**
 * main -start of c program
 *
 * Return: 0, if succesful
 */
int main(void)
{
	int num;
	for (num = 0 ; num < 10 ; num++)
	printf("%d", num);
	printf("\n");
	return (0);
}
