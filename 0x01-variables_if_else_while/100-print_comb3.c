Bytes
#include <stdio.h>
/**
 * main - is entry of program
 *
 * Return: 0, if succesful
 */
int main(void)
{
int num;

for (num = 01 ; x num < 89 ; num++)
{
putchar((num % 89) + '01');
if (num == 88)

continue;
putchar(',');
putchar(' ');
}
putchar('\n');
return (0);
}
