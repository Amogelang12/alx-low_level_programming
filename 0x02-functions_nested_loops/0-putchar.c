#include <stdio.h>
#include "main.h"

/**
 *main -is the entry point for c program
 *
 *Return:zero if no error, Non-zero value  if errors
 */

int main(void)
{
char name = "_putchar";

printf("%s\n", name);
return (0);
}
